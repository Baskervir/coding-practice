# 9063: 대지

### 문제 정보
- **문제 링크**: [https://www.acmicpc.net/problem/9063](https://www.acmicpc.net/problem/9063)

---

### 나의 풀이

#### 💡 핵심 아이디어
- 주어진 모든 점을 포함하는 가장 작은 직사각형의 넓이를 구하기
  - 주어진 모든 점들 중 최댓값과 최솟값의 차이를 각 가로와 세로의 길이로서 연산한다

#### 🤔 어려웠던 점 / 배운 점
- 초기값 설정
  - 최댓값을 가장 작은 정수로, 최솟값을 가장 큰 정수로 초기값을 설정한다
- `N = 1`일 경우 한 점만으로는 직사각형을 만들 수 없다.
  - 이 경우 0을 반환하도록 설계한다
- 좌표값은 `-10,000`부터 `10,000`이므로 `int`타입 범위 내에서 충분히 처리 가능

####  실행 결과
- **메모리**: 27024 KB
- **시간**: 244 ms

---

### 소스 코드
```java
import java.util.*;
import java.io.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        
        int N = Integer.parseInt(br.readLine());
        
        if (N <= 1) {
            if (N == 1) {
                br.readLine();
            }
            System.out.println(0);
            return;
        }

        int minX = Integer.MAX_VALUE;
        int minY = Integer.MAX_VALUE;
        int maxX = Integer.MIN_VALUE;
        int maxY = Integer.MIN_VALUE;
        
        for (int i = 0; i < N; i++) {
            StringTokenizer st = new StringTokenizer(br.readLine());
            
            int x = Integer.parseInt(st.nextToken());
            int y = Integer.parseInt(st.nextToken());
            
            if (x < minX) {
                minX = x;
            }
            if (x > maxX) {
                maxX = x;
            }
            
            if (y < minY) {
                minY = y;
            }
            if (y > maxY) {
                maxY = y;
            }
        }

        int width = maxX - minX;
        int height = maxY - minY;
        
        long area = (long) width * height;
        
        System.out.println(area);
    }
}
```