# 2581: 소수

### 문제 정보
- **문제 링크**: [https://www.acmicpc.net/problem/2581](https://www.acmicpc.net/problem/2581)

---

### 나의 풀이

#### 💡 핵심 아이디어
- `M`과 `N`사이의 소수를 모두 찾는다
- 찾은 소수들의 `합계`를 구한다
- 찾은 소수들 중 `최솟값`을 구한다
- 소수가 하나도 없을 경우 `-1`을 출력한다

#### 🤔 어려웠던 점 / 배운 점
- 소수 판별의 효율성
  - 어떤 수 `X`가 소수인지 판별할 때, `X`보다 작은 모든 수로 나누어 보는것은 비효율적이다
- 소수가 없을 때의 처리
  - `M`이상 `N`이하의 소수가 없을 경우, 합계 대신 `-1`만 출력하도록 하며, 최소값은 출력하지 않도록 처리

####  실행 결과
- **메모리**: 14184 KB
- **시간**: 108 ms

---

### 소스 코드
```java
import java.io.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        
        int M = Integer.parseInt(br.readLine());
        int N = Integer.parseInt(br.readLine());
        
        long sum = 0;
        int min = Integer.MAX_VALUE;
        boolean primeFound = false;
        
        for (int i = M; i <= N; i++) {
            if (isPrime(i)) {
                sum += i;
                
                if (!primeFound) {
                    min = i;
                    primeFound = true;
                }
            }
        }
        
        if (sum == 0) {
            System.out.println(-1);
        } else {
            System.out.println(sum);
            System.out.println(min);
        }
    }
    
    public static boolean isPrime(int x) {
        if (x < 2) {
            return false;
        }
        
        for (int i = 2; i * i <= x; i++) {
            if (x % i == 0) {
                return false;
            }
        }
        
        return true;
    }
}
```