# 1978: 소수 찾기

### 문제 정보
- **문제 링크**: [https://www.acmicpc.net/problem/1978](https://www.acmicpc.net/problem/1978)

---

### 나의 풀이

#### 💡 핵심 아이디어
- 자연수 `X`가 소수인지 판별하기 위해 2부터 `X - 1`까지의 수로 `X`를 나누어 보아 나뉘는지 확인

#### 🤔 어려웠던 점 / 배운 점
- ...

####  실행 결과
- **메모리**: 14088 KB
- **시간**: 100 ms

---

### 소스 코드
```java
import java.io.*;
import java.util.*;

public class Main {
    public static boolean isPrime(int number) {
        if (number < 2) {
            return false;
        }

        for (int i = 2; i < number; i++) {
            if (number % i == 0) {
                return false;
            }
        }

        return true;
    }

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        int N = Integer.parseInt(br.readLine());

        StringTokenizer st = new StringTokenizer(br.readLine(), " ");
        
        int primeCount = 0;

        for (int i = 0; i < N; i++) {
            int num = Integer.parseInt(st.nextToken());
            
            if (isPrime(num)) {
                primeCount++;
            }
        }

        System.out.println(primeCount);
    }
}
```