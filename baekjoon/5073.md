# 5073: 삼각형과 세 변

### 문제 정보
- **문제 링크**: [https://www.acmicpc.net/problem/5073](https://www.acmicpc.net/problem/5073)

---

### 나의 풀이

#### 💡 핵심 아이디어
- `while (!(line = br.readLine()).equals("0 0 0"))`입력으로 "0 0 0"이 들어오면 반복 종료
- `Arrays.sort(sides)`로 세 변을 오름차순으로 정렬
  - 가장 긴 변은 항상 `sides[2]`에 위치

#### 🤔 어려웠던 점 / 배운 점
- ...

####  실행 결과
- **메모리**: 14184 KB
- **시간**: 100 ms

---

### 소스 코드
```java
import java.io.*;
import java.util.*;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringBuilder sb = new StringBuilder();
        String line;
        
        while (!(line = br.readLine()).equals("0 0 0")) {
            StringTokenizer st = new StringTokenizer(line);
            
            int[] sides = new int[3];
            sides[0] = Integer.parseInt(st.nextToken());
            sides[1] = Integer.parseInt(st.nextToken());
            sides[2] = Integer.parseInt(st.nextToken());
            
            Arrays.sort(sides);
            
            int a = sides[0];
            int b = sides[1];
            int c = sides[2];
            
            String result;
            
            if (a + b <= c) {
                result = "Invalid";
            } else {
                if (a == b && b == c) {
                    result = "Equilateral";
                } else if (a == b || b == c || a == c) {
                    result = "Isosceles";
                } else {
                    result = "Scalene";
                }
            }
            
            sb.append(result).append('\n');
        }
        
        System.out.print(sb.toString());
    }
}
```